<ul class="tabs">
  <li><a href="/rss">Find Prospects</a></li>
  <li><a href="/approved_links">Qualify Leads</a></li>
  <li class="active"><a href="/leads">Nurture Clients</a></li>
</ul>
<script>
	$(document).ready(function() {
		/*$(".approved_links a.hide").on("click", function() {
			var row = $(this).parents("tr");
			row.next().remove();
			row.remove();
		});*/
	});
</script>

<div class="content">


<%= link_to 'New Workshop Exclusive Lead', new_exclusive_path, class: "submit" %> <%= link_to 'New Lead', new_lead_path, class: "submit" %> <%= link_to 'New RFP', new_rfp_path, class: "submit" %>

<br><br><br>


Today's Leads <i class="fa fa-long-arrow-right"></i> <a href="#">Past Leads</a> <i class="fa fa-long-arrow-right"></i> <a href="#">Clients</a>


<h1>Workshop-exclusive leads</h1>

<table>
  <tbody>

    <% @exclusives.today.most_recently_updated.each do |exclusive| %>
      <% if exclusive.present? %>
        <tr>
          <td style="padding-right: 2em;"><span style="color: #19963C; font-size: 14px; font-weight: 500;">Created Today!</span> <%= link_to exclusive.title, edit_exclusive_path(exclusive) %></td>
          <td><%= link_to 'Destroy', exclusive, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    <% end %>

  </tbody>
  <tbody>
    <th><p style="color: #888888; font-weight: normal; text-align: left;"><br>All Workshop-Exclusive Leads:</p></th>
    <% @exclusives.most_recently_updated.each do |exclusive| %>
      <tr>
        <td style="padding-right: 2em;"><%= link_to exclusive.title, edit_exclusive_path(exclusive) %></td>
        <td><%= link_to 'Destroy', exclusive, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>


<h1>Workshop leads</h1>

<table style="margin: 0 0 7em 0;">
  <tbody>
    <% @leads.today.most_recently_updated.each do |lead| %>
      <% if lead.present? %>
        <tr>
          <td style="padding-right: 2em;"><span style="color: #19963C; font-size: 14px; font-weight: 500;">Created Today!</span> <%= link_to lead.title, edit_lead_path(lead) %></td>
          <td><%= link_to 'Destroy', lead, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>

  <tbody>
    <th colspan="3"><p style="color: #888888; font-weight: normal; text-align: left;"><br>All Leads:</p></th>
    <% @leads.most_recently_updated.each do |lead| %>
      <tr>
        <td style="padding-right: 2em;"><%= link_to lead.title, edit_lead_path(lead) %></td>
        <td><%= link_to 'Destroy', lead, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


</div>