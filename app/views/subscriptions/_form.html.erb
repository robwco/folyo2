
	<script>
	$(function() {
	  $('#pictureInput').on('change', function(event) {
		var files = event.target.files;
		var image = files[0]
		// here's the file size
		console.log(image.size);
		var reader = new FileReader();
		reader.onload = function(file) {
		  var img = new Image();
		  console.log(file);
		  img.src = file.target.result;
		  $('#target').html(img);
		}
		reader.readAsDataURL(image);
		console.log(files);
	  });
	});
	</script>
	
	<style>
		
	
	</style>

	<%= form_for :user, url: subscriptions_path, html: { multipart: true, id: 'new_user', class: 'form-focus' } do |f| %>	

		<h3 class="form-header">Your name</h3>
		<%= f.text_field :name, placeholder: 'Name', :style => "border: 1px solid #dd8687 !important;"	%>
		<p style="color:#d9666a; margin-left: .25em; font-weight: 400; font-size: 13px; margin: -.25em 0 0;">Enter your name</p>

		<h3 class="form-header">Your work email</h3>
		<p class="required">Won't be shown publicly</p>
		<%= f.email_field :email, placeholder: "Email", :style => "border: 1px solid #dd8687 !important;" %>
		<p style="color:#d9666a; margin-left: .25em; font-weight: 400; font-size: 13px; margin: -.25em 0 0;">Enter your email</p>

<!--
		<div class="upload-photo-wrap" style="display: table; width: 100%;">
			<div class="upload-photo">
				<h3 class="form-header">Your picture</h3>
				<p class="required">(Optional) but we do like seeing fresh faces</p>
			</div>

			<div class="preview-photo">
				<div id="target"></div>
				<%= f.file_field :photo, :id => "pictureInput", :style => "width: 200px;"	%>
			</div>
		</div>-->



		<h3 class="form-header">Create a password</h3>
		<p class="required">Must have at least 8 characters</p>
			<p style="margin:0;">
				<% if @validatable %>
					<small><em>(<%= @minimum_password_length %> characters minimum)</em></small>
				<% end %>
			</p>

			<%= f.password_field :password, autocomplete: "off", placeholder: 'Password', :style => "border: 1px solid #dd8687 !important;" %>
					<p style="color:#d9666a; margin-left: .25em; font-weight: 400; font-size: 13px; margin: -.25em 0 0;">Create a password with at least 8 characters</p>

		<div style="margin: 1em 0;">
			<a href="#">
				<%= f.submit "Sign up", id: 'form-submit', data: { :"drip-attribute" => 'sign-up-button' } %>
			</a>
		</div>


		<p style="font-size: 14px;">Already have an account? <a href="/users/sign_in">Log in</a></p>

		<%= hidden_field_tag "plan", @plan ? @plan.id : "" %>
	<% end %>
	

