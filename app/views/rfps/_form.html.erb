
<%= form_for(@rfp) do |f| %>
  <% if @rfp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rfp.errors.count, "error") %> prohibited this rfp from being saved:</h2>

      <ul>
      <% @rfp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
		<%= f.file_field :file %>
  </div>




  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>