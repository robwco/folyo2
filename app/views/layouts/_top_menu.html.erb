<nav class="select-none bg-white lg:flex lg:items-stretch w-full pin-t border-b-2 font-sans shadow">
  <div class="flex flex-no-shrink items-stretch">
		<% if admin_signed_in? %>
			<%= link_to "Admin Home", admin_root_path, :class=> "flex-no-grow flex-no-shrink relative py-4 px-4 leading-normal text-grey no-underline flex items-center text-base"  %>

		<% elsif user_signed_in? %>
			<%= link_to "Home", url_for(root_path), :class=> "flex-no-grow flex-no-shrink relative py-4 px-4 leading-normal text-grey no-underline flex items-center text-base" %>
		<% else %>
			<%= link_to "Home".html_safe, url_for(root_path), :class=> "flex-no-grow flex-no-shrink relative py-4 px-4 leading-normal text-grey no-underline flex items-center text-base" %>
		<% end %>
  </div>

  <div class="lg:flex lg:items-stretch lg:flex-no-shrink lg:flex-grow">
    <div class="lg:flex lg:items-stretch lg:justify-end ml-auto">
			<% if admin_signed_in? %>
				<%= link_to "Log out", destroy_admin_session_path, method: :delete%>

			<% elsif user_signed_in? %>
					<% if current_user.present? && current_user.client? %>
						<%= link_to "New Project", new_project_path, :class=> "flex-no-grow flex-no-shrink relative py-4 px-4 leading-normal text-grey no-underline flex items-center text-base"  %>
					<% end %>

				<%= link_to "<i class='fa fa-envelope text-grey-dark #{ "unread" if @unread_message_count > 0}' aria-hidden='true'></i>Inbox (#{@unread_message_count})".html_safe, inbox_projects_path(archived: @unread_message_count.zero? || nil), :class=> "flex-no-grow flex-no-shrink relative py-4 px-4 leading-normal text-grey no-underline flex items-center text-base" %>
				<%= link_to 'Profile', profile_path(current_user), :class=> "flex-no-grow flex-no-shrink relative py-4 px-4 leading-normal text-grey no-underline flex items-center text-base" %>
				<%= link_to "Log out", destroy_user_session_path, method: :delete, :class=> "flex-no-grow flex-no-shrink relative py-4 px-4 leading-normal text-grey no-underline flex items-center text-base" %>

			<% else %>
				<%= link_to 'Sign up', new_subscription_path, :class => "flex-no-grow flex-no-shrink relative py-2 px-4 leading-normal text-grey-darker no-underline flex items-center hover:bg-grey-lighter" %>
				<%= link_to "Log in", new_user_session_path, :class => "flex-no-grow flex-no-shrink relative py-2 px-4 leading-normal text-grey-darker no-underline flex items-center hover:bg-grey-lighter" %>
			<% end %>
    </div>
  </div>
</nav>
