<%= form_for(@job_source) do |f| %>
  <% if @job_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_source.errors.count, "error") %> prohibited this job_source from being saved:</h2>

      <ul>
      <% @job_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
